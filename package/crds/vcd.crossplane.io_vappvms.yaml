---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: vappvms.vcd.crossplane.io
spec:
  group: vcd.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - vcd
    kind: VAppVm
    listKind: VAppVmList
    plural: vappvms
    singular: vappvm
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VAppVm is the Schema for the VAppVms API. <no value>
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VAppVmSpec defines the desired state of VAppVm
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicy field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  acceptAllEulas:
                    description: Automatically accept EULA if OVA has it
                    type: boolean
                  bootImage:
                    description: Media name to add as boot image.
                    type: string
                  bootImageId:
                    description: The URN of the media to use as boot image.
                    type: string
                  catalogName:
                    description: The catalog name in which to find the given vApp
                      Template or media for boot_image
                    type: string
                  computerName:
                    description: Computer name to assign to this virtual machine
                    type: string
                  cpuCores:
                    description: The number of cores per socket
                    type: number
                  cpuHotAddEnabled:
                    description: True if the virtual machine supports addition of
                      virtual CPUs while powered on.
                    type: boolean
                  cpuLimit:
                    description: The limit for how much of CPU can be consumed on
                      the underlying virtualization infrastructure. This is only valid
                      when the resource allocation is not unlimited.
                    type: number
                  cpuPriority:
                    description: Pre-determined relative priorities according to which
                      the non-reserved portion of this resource is made available
                      to the virtualized workload
                    type: string
                  cpuReservation:
                    description: The amount of MHz reservation on the underlying virtualization
                      infrastructure
                    type: number
                  cpuShares:
                    description: Custom priority for the resource. This is a read-only,
                      unless the `cpu_priority` is CUSTOM
                    type: number
                  cpus:
                    description: The number of virtual CPUs to allocate to the VM
                    type: number
                  customization:
                    description: Guest customization block
                    items:
                      properties:
                        adminPasswordSecretRef:
                          description: Manually specify admin password
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        allowLocalAdminPassword:
                          description: Allow local administrator password
                          type: boolean
                        autoGeneratePassword:
                          description: Auto generate password
                          type: boolean
                        changeSid:
                          description: '''true'' value will change SID. Applicable
                            only for Windows VMs'
                          type: boolean
                        enabled:
                          description: '''true'' value will enable guest customization.
                            It may occur on first boot or when ''force'' is used'
                          type: boolean
                        force:
                          description: '''true'' value will cause the VM to reboot
                            on every ''apply'' operation'
                          type: boolean
                        initscript:
                          description: Script to run on initial boot or with customization.force=true
                            set
                          type: string
                        joinDomain:
                          description: Enable this VM to join a domain
                          type: boolean
                        joinDomainAccountOu:
                          description: Account organizational unit for domain name
                            join
                          type: string
                        joinDomainName:
                          description: Custom domain name for join
                          type: string
                        joinDomainPasswordSecretRef:
                          description: Password for custom domain name join
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        joinDomainUser:
                          description: Username for custom domain name join
                          type: string
                        joinOrgDomain:
                          description: Use organization's domain for joining
                          type: boolean
                        mustChangePasswordOnFirstLogin:
                          description: Require Administrator to change password on
                            first login
                          type: boolean
                        numberOfAutoLogons:
                          description: Number of times to log on automatically. '0'
                            - disabled.
                          type: number
                      type: object
                    type: array
                  description:
                    description: The VM description
                    type: string
                  disk:
                    items:
                      properties:
                        busNumber:
                          description: Bus number on which to place the disk controller
                          type: string
                        name:
                          description: Independent disk name
                          type: string
                        unitNumber:
                          description: Unit number (slot) on the bus specified by
                            BusNumber
                          type: string
                      required:
                      - busNumber
                      - name
                      - unitNumber
                      type: object
                    type: array
                  exposeHardwareVirtualization:
                    description: Expose hardware-assisted CPU virtualization to guest
                      OS.
                    type: boolean
                  guestProperties:
                    additionalProperties:
                      type: string
                    description: Key/value settings for guest properties
                    type: object
                  hardwareVersion:
                    description: Virtual Hardware Version (e.g.`vmx-14`, `vmx-13`,
                      `vmx-12`, etc.)
                    type: string
                  href:
                    description: VM Hyper Reference
                    type: string
                  memory:
                    description: The amount of RAM (in MB) to allocate to the VM
                    type: number
                  memoryHotAddEnabled:
                    description: True if the virtual machine supports addition of
                      memory while powered on.
                    type: boolean
                  memoryLimit:
                    description: The limit for how much of memory can be consumed
                      on the underlying virtualization infrastructure. This is only
                      valid when the resource allocation is not unlimited.
                    type: number
                  memoryPriority:
                    description: Pre-determined relative priorities according to which
                      the non-reserved portion of this resource is made available
                      to the virtualized workload
                    type: string
                  memoryReservation:
                    description: The amount of RAM (in MB) reservation on the underlying
                      virtualization infrastructure
                    type: number
                  memoryShares:
                    description: Custom priority for the resource. This is a read-only,
                      unless the `memory_priority` is CUSTOM
                    type: number
                  metadata:
                    additionalProperties:
                      type: string
                    description: Key value map of metadata to assign to this VM
                    type: object
                  metadataEntry:
                    description: Metadata entries for the given VM
                    items:
                      properties:
                        isSystem:
                          description: Domain for this metadata entry. true if it
                            belongs to SYSTEM, false if it belongs to GENERAL
                          type: boolean
                        key:
                          description: Key of this metadata entry. Required if the
                            metadata entry is not empty
                          type: string
                        type:
                          description: 'Type of this metadata entry. One of: ''MetadataStringValue'',
                            ''MetadataNumberValue'', ''MetadataBooleanValue'', ''MetadataDateTimeValue'''
                          type: string
                        userAccess:
                          description: 'User access level for this metadata entry.
                            One of: ''READWRITE'', ''READONLY'', ''PRIVATE'''
                          type: string
                        value:
                          description: Value of this metadata entry. Required if the
                            metadata entry is not empty
                          type: string
                      type: object
                    type: array
                  name:
                    description: A name for the VM, unique within the vApp
                    type: string
                  network:
                    description: A block to define network interface. Multiple can
                      be used.
                    items:
                      properties:
                        adapterType:
                          description: Network card adapter type. (e.g. 'E1000', 'E1000E',
                            'SRIOVETHERNETCARD', 'VMXNET3', 'PCNet32')
                          type: string
                        connected:
                          description: It defines if NIC is connected or not.
                          type: boolean
                        ip:
                          description: IP of the VM. Settings depend on `ip_allocation_mode`.
                            Omitted or empty for DHCP, POOL, NONE. Required for MANUAL
                          type: string
                        ipAllocationMode:
                          description: IP address allocation mode. One of POOL, DHCP,
                            MANUAL, NONE
                          type: string
                        isPrimary:
                          description: Set to true if network interface should be
                            primary. First network card in the list will be primary
                            by default
                          type: boolean
                        mac:
                          description: Mac address of network interface
                          type: string
                        name:
                          description: Name of the network this VM should connect
                            to. Always required except for `type` `NONE`
                          type: string
                        type:
                          description: 'Network type to use: ''vapp'', ''org'' or
                            ''none''. Use ''vapp'' for vApp network, ''org'' to attach
                            Org VDC network. ''none'' for empty NIC.'
                          type: string
                      required:
                      - type
                      type: object
                    type: array
                  networkDhcpWaitSeconds:
                    description: Optional number of seconds to try and wait for DHCP
                      IP (valid for 'network' block only)
                    type: number
                  org:
                    description: The name of organization to use, optional if defined
                      at provider level. Useful when connected as sysadmin working
                      across different organizations
                    type: string
                  osType:
                    description: Operating System type. Possible values can be found
                      in documentation.
                    type: string
                  overrideTemplateDisk:
                    description: A block to match internal_disk interface in template.
                      Multiple can be used. Disk will be matched by bus_type, bus_number
                      and unit_number.
                    items:
                      properties:
                        busNumber:
                          description: The number of the SCSI or IDE controller itself.
                          type: number
                        busType:
                          description: 'The type of disk controller. Possible values:
                            ide, parallel( LSI Logic Parallel SCSI), sas(LSI Logic
                            SAS (SCSI)), paravirtual(Paravirtual (SCSI)), sata, nvme'
                          type: string
                        iops:
                          description: Specifies the IOPS for the disk. Default is
                            0.
                          type: number
                        sizeInMb:
                          description: The size of the disk in MB.
                          type: number
                        storageProfile:
                          description: Storage profile to override the VM default
                            one
                          type: string
                        unitNumber:
                          description: The device number on the SCSI or IDE controller
                            of the disk.
                          type: number
                      required:
                      - busNumber
                      - busType
                      - sizeInMb
                      - unitNumber
                      type: object
                    type: array
                  placementPolicyId:
                    description: VM placement policy ID. Has to be assigned to Org
                      VDC.
                    type: string
                  powerOn:
                    description: A boolean value stating if this VM should be powered
                      on
                    type: boolean
                  preventUpdatePowerOff:
                    description: True if the update of resource should fail when virtual
                      machine power off needed.
                    type: boolean
                  securityTags:
                    description: Security tags to assign to this VM
                    items:
                      type: string
                    type: array
                  sizingPolicyId:
                    description: VM sizing policy ID. Has to be assigned to Org VDC.
                    type: string
                  storageProfile:
                    description: Storage profile to override the default one
                    type: string
                  templateName:
                    description: The name of the vApp Template to use
                    type: string
                  vappName:
                    description: The vApp this VM belongs to - Required, unless it
                      is a standalone VM
                    type: string
                  vappTemplateId:
                    description: The URN of the vApp Template to use
                    type: string
                  vdc:
                    description: The name of VDC to use, optional if defined at provider
                      level
                    type: string
                  vmNameInTemplate:
                    description: The name of the VM in vApp Template to use. In cases
                      when vApp template has more than one VM
                    type: string
                type: object
              managementPolicy:
                default: FullControl
                description: 'THIS IS AN ALPHA FIELD. Do not use it in production.
                  It is not honored unless the relevant Crossplane feature flag is
                  enabled, and may be changed or removed without notice. ManagementPolicy
                  specifies the level of control Crossplane has over the managed external
                  resource. This field is planned to replace the DeletionPolicy field
                  in a future release. Currently, both could be set independently
                  and non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - FullControl
                - ObserveOnly
                - OrphanOnDelete
                type: string
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: name is a required parameter
              rule: self.managementPolicy == 'ObserveOnly' || has(self.forProvider.name)
            - message: vappName is a required parameter
              rule: self.managementPolicy == 'ObserveOnly' || has(self.forProvider.vappName)
          status:
            description: VAppVmStatus defines the observed state of VAppVm.
            properties:
              atProvider:
                properties:
                  acceptAllEulas:
                    description: Automatically accept EULA if OVA has it
                    type: boolean
                  bootImage:
                    description: Media name to add as boot image.
                    type: string
                  bootImageId:
                    description: The URN of the media to use as boot image.
                    type: string
                  catalogName:
                    description: The catalog name in which to find the given vApp
                      Template or media for boot_image
                    type: string
                  computerName:
                    description: Computer name to assign to this virtual machine
                    type: string
                  cpuCores:
                    description: The number of cores per socket
                    type: number
                  cpuHotAddEnabled:
                    description: True if the virtual machine supports addition of
                      virtual CPUs while powered on.
                    type: boolean
                  cpuLimit:
                    description: The limit for how much of CPU can be consumed on
                      the underlying virtualization infrastructure. This is only valid
                      when the resource allocation is not unlimited.
                    type: number
                  cpuPriority:
                    description: Pre-determined relative priorities according to which
                      the non-reserved portion of this resource is made available
                      to the virtualized workload
                    type: string
                  cpuReservation:
                    description: The amount of MHz reservation on the underlying virtualization
                      infrastructure
                    type: number
                  cpuShares:
                    description: Custom priority for the resource. This is a read-only,
                      unless the `cpu_priority` is CUSTOM
                    type: number
                  cpus:
                    description: The number of virtual CPUs to allocate to the VM
                    type: number
                  customization:
                    description: Guest customization block
                    items:
                      properties:
                        allowLocalAdminPassword:
                          description: Allow local administrator password
                          type: boolean
                        autoGeneratePassword:
                          description: Auto generate password
                          type: boolean
                        changeSid:
                          description: '''true'' value will change SID. Applicable
                            only for Windows VMs'
                          type: boolean
                        enabled:
                          description: '''true'' value will enable guest customization.
                            It may occur on first boot or when ''force'' is used'
                          type: boolean
                        force:
                          description: '''true'' value will cause the VM to reboot
                            on every ''apply'' operation'
                          type: boolean
                        initscript:
                          description: Script to run on initial boot or with customization.force=true
                            set
                          type: string
                        joinDomain:
                          description: Enable this VM to join a domain
                          type: boolean
                        joinDomainAccountOu:
                          description: Account organizational unit for domain name
                            join
                          type: string
                        joinDomainName:
                          description: Custom domain name for join
                          type: string
                        joinDomainUser:
                          description: Username for custom domain name join
                          type: string
                        joinOrgDomain:
                          description: Use organization's domain for joining
                          type: boolean
                        mustChangePasswordOnFirstLogin:
                          description: Require Administrator to change password on
                            first login
                          type: boolean
                        numberOfAutoLogons:
                          description: Number of times to log on automatically. '0'
                            - disabled.
                          type: number
                      type: object
                    type: array
                  description:
                    description: The VM description
                    type: string
                  disk:
                    items:
                      properties:
                        busNumber:
                          description: Bus number on which to place the disk controller
                          type: string
                        name:
                          description: Independent disk name
                          type: string
                        sizeInMb:
                          description: The size of the disk in MB.
                          type: number
                        unitNumber:
                          description: Unit number (slot) on the bus specified by
                            BusNumber
                          type: string
                      type: object
                    type: array
                  exposeHardwareVirtualization:
                    description: Expose hardware-assisted CPU virtualization to guest
                      OS.
                    type: boolean
                  guestProperties:
                    additionalProperties:
                      type: string
                    description: Key/value settings for guest properties
                    type: object
                  hardwareVersion:
                    description: Virtual Hardware Version (e.g.`vmx-14`, `vmx-13`,
                      `vmx-12`, etc.)
                    type: string
                  href:
                    description: VM Hyper Reference
                    type: string
                  id:
                    type: string
                  internalDisk:
                    description: A block will show internal disk details
                    items:
                      properties:
                        busNumber:
                          type: number
                        busType:
                          type: string
                        diskId:
                          type: string
                        iops:
                          type: number
                        sizeInMb:
                          type: number
                        storageProfile:
                          type: string
                        thinProvisioned:
                          type: boolean
                        unitNumber:
                          type: number
                      type: object
                    type: array
                  memory:
                    description: The amount of RAM (in MB) to allocate to the VM
                    type: number
                  memoryHotAddEnabled:
                    description: True if the virtual machine supports addition of
                      memory while powered on.
                    type: boolean
                  memoryLimit:
                    description: The limit for how much of memory can be consumed
                      on the underlying virtualization infrastructure. This is only
                      valid when the resource allocation is not unlimited.
                    type: number
                  memoryPriority:
                    description: Pre-determined relative priorities according to which
                      the non-reserved portion of this resource is made available
                      to the virtualized workload
                    type: string
                  memoryReservation:
                    description: The amount of RAM (in MB) reservation on the underlying
                      virtualization infrastructure
                    type: number
                  memoryShares:
                    description: Custom priority for the resource. This is a read-only,
                      unless the `memory_priority` is CUSTOM
                    type: number
                  metadata:
                    additionalProperties:
                      type: string
                    description: Key value map of metadata to assign to this VM
                    type: object
                  metadataEntry:
                    description: Metadata entries for the given VM
                    items:
                      properties:
                        isSystem:
                          description: Domain for this metadata entry. true if it
                            belongs to SYSTEM, false if it belongs to GENERAL
                          type: boolean
                        key:
                          description: Key of this metadata entry. Required if the
                            metadata entry is not empty
                          type: string
                        type:
                          description: 'Type of this metadata entry. One of: ''MetadataStringValue'',
                            ''MetadataNumberValue'', ''MetadataBooleanValue'', ''MetadataDateTimeValue'''
                          type: string
                        userAccess:
                          description: 'User access level for this metadata entry.
                            One of: ''READWRITE'', ''READONLY'', ''PRIVATE'''
                          type: string
                        value:
                          description: Value of this metadata entry. Required if the
                            metadata entry is not empty
                          type: string
                      type: object
                    type: array
                  name:
                    description: A name for the VM, unique within the vApp
                    type: string
                  network:
                    description: A block to define network interface. Multiple can
                      be used.
                    items:
                      properties:
                        adapterType:
                          description: Network card adapter type. (e.g. 'E1000', 'E1000E',
                            'SRIOVETHERNETCARD', 'VMXNET3', 'PCNet32')
                          type: string
                        connected:
                          description: It defines if NIC is connected or not.
                          type: boolean
                        ip:
                          description: IP of the VM. Settings depend on `ip_allocation_mode`.
                            Omitted or empty for DHCP, POOL, NONE. Required for MANUAL
                          type: string
                        ipAllocationMode:
                          description: IP address allocation mode. One of POOL, DHCP,
                            MANUAL, NONE
                          type: string
                        isPrimary:
                          description: Set to true if network interface should be
                            primary. First network card in the list will be primary
                            by default
                          type: boolean
                        mac:
                          description: Mac address of network interface
                          type: string
                        name:
                          description: Name of the network this VM should connect
                            to. Always required except for `type` `NONE`
                          type: string
                        type:
                          description: 'Network type to use: ''vapp'', ''org'' or
                            ''none''. Use ''vapp'' for vApp network, ''org'' to attach
                            Org VDC network. ''none'' for empty NIC.'
                          type: string
                      type: object
                    type: array
                  networkDhcpWaitSeconds:
                    description: Optional number of seconds to try and wait for DHCP
                      IP (valid for 'network' block only)
                    type: number
                  org:
                    description: The name of organization to use, optional if defined
                      at provider level. Useful when connected as sysadmin working
                      across different organizations
                    type: string
                  osType:
                    description: Operating System type. Possible values can be found
                      in documentation.
                    type: string
                  overrideTemplateDisk:
                    description: A block to match internal_disk interface in template.
                      Multiple can be used. Disk will be matched by bus_type, bus_number
                      and unit_number.
                    items:
                      properties:
                        busNumber:
                          description: The number of the SCSI or IDE controller itself.
                          type: number
                        busType:
                          description: 'The type of disk controller. Possible values:
                            ide, parallel( LSI Logic Parallel SCSI), sas(LSI Logic
                            SAS (SCSI)), paravirtual(Paravirtual (SCSI)), sata, nvme'
                          type: string
                        iops:
                          description: Specifies the IOPS for the disk. Default is
                            0.
                          type: number
                        sizeInMb:
                          description: The size of the disk in MB.
                          type: number
                        storageProfile:
                          description: Storage profile to override the VM default
                            one
                          type: string
                        unitNumber:
                          description: The device number on the SCSI or IDE controller
                            of the disk.
                          type: number
                      type: object
                    type: array
                  placementPolicyId:
                    description: VM placement policy ID. Has to be assigned to Org
                      VDC.
                    type: string
                  powerOn:
                    description: A boolean value stating if this VM should be powered
                      on
                    type: boolean
                  preventUpdatePowerOff:
                    description: True if the update of resource should fail when virtual
                      machine power off needed.
                    type: boolean
                  securityTags:
                    description: Security tags to assign to this VM
                    items:
                      type: string
                    type: array
                  sizingPolicyId:
                    description: VM sizing policy ID. Has to be assigned to Org VDC.
                    type: string
                  status:
                    description: Shows the status code of the VM
                    type: number
                  statusText:
                    description: Shows the status of the VM
                    type: string
                  storageProfile:
                    description: Storage profile to override the default one
                    type: string
                  templateName:
                    description: The name of the vApp Template to use
                    type: string
                  vappName:
                    description: The vApp this VM belongs to - Required, unless it
                      is a standalone VM
                    type: string
                  vappTemplateId:
                    description: The URN of the vApp Template to use
                    type: string
                  vdc:
                    description: The name of VDC to use, optional if defined at provider
                      level
                    type: string
                  vmNameInTemplate:
                    description: The name of the VM in vApp Template to use. In cases
                      when vApp template has more than one VM
                    type: string
                  vmType:
                    description: 'Type of VM: either ''vcd_vapp_vm'' or ''vcd_vm'''
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
